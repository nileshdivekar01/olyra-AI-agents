## In this MVp we make the UI responsive and add the data frame agent to give the user query answer on the csv file data and texual data.
- tabular , chart etc. i give answer.

Need Improvement: 
currently it is not able to answer the data analytics queries because of create two con _id so i will change it next mvp

Note:  Frontend/ folder has old UI which is not responsive 
and 
updated_frontend/
it is responsive UI with tabular, chart,etc.

